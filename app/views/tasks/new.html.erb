<%= form_for(@task) do |f| %>
  <p><%= f.label :title %>
  <%= f.text_field :title %></p>
  <%= f.label :details %>
  <%= f.text_area :details %>
  <%= f.label :completed %>
  <%= f.check_box :completed %>
  <%= f.submit %>

<% end %>
